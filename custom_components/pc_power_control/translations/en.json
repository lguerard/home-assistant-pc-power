{
  "config": {
    "step": {
      "user": {
        "title": "PC Power Control",
        "description": "Configure your PC's MAC and IP address",
        "data": {
          "name": "Name",
          "host": "Host",
          "mac": "MAC Address",
          "username": "SSH Username",
          "password": "SSH Password",
          "ssh_port": "SSH Port",
          "ssh_timeout": "SSH Timeout (seconds)"
        }
      }
    }
  },
  "options": {
    "step": {
      "user": {
        "title": "PC Power Control Options",
        "description": "Update your PC configuration",
        "data": {
          "name": "Name",
          "host": "Host",
          "mac": "MAC Address",
          "username": "SSH Username",
          "password": "SSH Password",
          "ssh_port": "SSH Port",
          "ssh_timeout": "SSH Timeout (seconds)"
        }
      }
    }
  },
  "services": {
    "send_ssh_command": {
      "name": "Send SSH Command",
      "description": "Send a custom command to the PC via SSH (automatically uses integration configuration)",
      "fields": {
        "command": {
          "name": "Command",
          "description": "The command to execute on the remote PC"
        },
        "timeout": {
          "name": "Timeout",
          "description": "Command timeout in seconds (optional)"
        },
        "pc_name": {
          "name": "PC Name",
          "description": "Name of the PC to send command to (optional if only one PC configured)"
        }
      }
    }
  },
  "entity": {
    "switch": {
      "monitor_timeout": {
        "name": "Monitor Timeout",
        "state": {
          "on": "30 minutes",
          "off": "Never"
        }
      }
    }
  },
  "flow_error": {
    "duplicate_host_or_mac": "This IP or MAC address is already configured."
  }
}
